arr=[1,2,3,4,5,6,7,8]
index=0
def setup():
    size(500,500)
    textSize(100)
def draw():
    text(arr[index],250,250)
def keyPressed():
    background(0)
    global index
    if key=='z' and index!=0:
        index=index-1
    if key=='x' and index!=len(arr)-1 :
         index=index+1
