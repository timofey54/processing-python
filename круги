x=250
y=250
def setup():
    size(500,500)
def draw():
    fill(random(0,255),random(0,255),random(0,255))
    global x,y
    for step in range(500,0,-50):     
        circle(x,y,step)
        step=step-50
    
