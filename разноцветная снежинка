def setup():
    size(1000,1000)
    translate(500,500)
    for y in range(0,360,45):
        rotate(radians(y))
        for x in range(8):
            fill(random(0,255),random(0,255),random(0,255))
            circle(x*40,0,50)
#нужно  по обратному рассуждать
